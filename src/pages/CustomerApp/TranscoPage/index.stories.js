import React from 'react';

import TranscoPage from '.';

export default {
	title: 'Pages/TransoProfile',
	component: TranscoPage
};

const Template = args => <TranscoPage {...args} />;

export const Default = Template.bind({});
